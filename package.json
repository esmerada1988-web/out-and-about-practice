{
  "name": "out-and-about-practice",
  "version": "1.0.0",
  "description": "Interactive English vocabulary practice with score tracking",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed - static HTML files'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
